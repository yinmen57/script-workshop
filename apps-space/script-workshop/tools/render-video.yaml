id: render-video
name: 生成成片视频
description: 调用赏舞视频能力，为已确认的叙事空间成片提示词生成一段视频（D1）。
risk_level: medium
entrypoint: src/tools.py:render_video
parameters:
  type: object
  properties:
    video_prompt_id:
      type: string
  required: [video_prompt_id]
